let ta = DateTime.now();

let total = 0;
let i = 0;
while (i < 9999) {
  let j = 0;
  while (j < 9999) {
    total += i * j;
    j++;
  }
  i++;
}

let tb = DateTime.now();

puts("\nTime: ", Math.round(DateTime.milliseconds(tb - ta)), "ms");
puts("Total: ", total);
