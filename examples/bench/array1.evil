t = ticks()

total = [0]
for i = 0 to 9999
  for j = 0 to 9999
    total[0] += i * j // @ = Direct acccess, faster than without it

writeln('\nTime: ', ticks() - t, 'ms') 
writeln('Total: ', total[0])
