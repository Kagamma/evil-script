v1 = 1
v2 = 2

fn test() {
  v1 = 101
  local v2 = 102
  {
    local v2 = 1002
    writeln(v2) // should be 1002
  }
  writeln(v1) // should be 101
  writeln(v2) // shoule be 102
}

test()
writeln(v1) // should be 101
writeln(v2) // shoule be 2